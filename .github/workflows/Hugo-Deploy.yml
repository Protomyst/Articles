name: Hugo Deploy

on:
  push:
    branches:
      - main  # 或者你的主分支名称
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Hugo PaperMod Deploy
      uses: Protomyst/Hugo-PaperMod-Deploy@1.0.4
