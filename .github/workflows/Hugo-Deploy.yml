name: Hugo Deploy

on:
  push:
    branches:
      - main  # 或者你的主分支名称
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Hugo Deploy
        uses: Protomyst/your-repo-name@v1.0.3
